from __future__ import annotations


def create_calendar_events(events: list[dict]) -> dict:
    return {
        "tool": "calendar_mcp",
        "status": "stub",
        "created": 0,
        "events": events,
    }
